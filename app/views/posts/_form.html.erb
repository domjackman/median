<%= simple_form_for @post do |f| %>
	<%= f.input :title %>
	<%= f.input :body %>
	<%= f.input :image %>
	<%= f.input :category_id, collection: Category.all %>
	<%= f.input :tag_list %>
	<%= f.button :submit %>
<% end %> 